let rows=[],selected=new Set(),assignment=null; const $=id=>document.getElementById(id); const log=m=>{$('log').textContent=`${new Date().toLocaleTimeString()}  ${m}\n`+$('log').textContent;$('status').textContent=m};
setInterval(()=>$('clock').textContent=new Date().toISOString().slice(11,16)+'Z',1000);
document.querySelectorAll('nav button').forEach(b=>b.onclick=()=>{document.querySelectorAll('nav button,.tab').forEach(x=>x.classList.remove('active'));b.classList.add('active');$(b.dataset.tab).classList.add('active')});
function render(){const f=[$('fac').value,$('fair').value,$('fdep').value,$('farr').value].map(x=>x.trim().toUpperCase());$('tbody').innerHTML='';rows.forEach((r,i)=>{if(f[0]&&!r.ac.includes(f[0])||f[1]&&!r.flight.includes(f[1])||f[2]&&!r.dep.includes(f[2])||f[3]&&!r.arr.includes(f[3]))return;let tr=document.createElement('tr');if(selected.has(i))tr.className='selected';tr.innerHTML=[r.dof,r.ac,r.reg,r.flight,r.dep,r.arr,r.std,r.sta].map(x=>`<td>${x||'—'}</td>`).join('');tr.onclick=()=>{selected.has(i)?selected.delete(i):selected.add(i);render()};$('tbody').appendChild(tr)})}
['fac','fair','fdep','farr'].forEach(x=>$(x).oninput=render);
$('parse').onclick=async()=>{log('Parsing FR24 history…');let r=await fetch('/api/parse-fr24',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({text:$('paste').value})});let j=await r.json();rows=j.rows||[];selected.clear();render();log(`Parsed ${rows.length} flight-history row(s).`)};
function fr24(kind){let a=$('airport').value.trim().toLowerCase().replace(/^k(?=[a-z]{3}$)/,'');if(a)open(`https://www.flightradar24.com/data/airports/${a}/${kind}`,'_blank')}$('dep').onclick=()=>fr24('departures');$('arr').onclick=()=>fr24('arrivals');
$('randomTrip').onclick=()=>{let n=Math.max(1,Math.min(+$('legs').value||1,rows.length));if(!rows.length)return log('No history rows available.');let start=Math.floor(Math.random()*(rows.length-n+1)),pick=rows.slice(start,start+n);$('tripout').classList.remove('empty');$('tripout').textContent=pick.map((r,i)=>`${i+1}. ${r.flight}   ${r.dep} → ${r.arr}   ${r.reg} / ${r.ac}   ${r.std}–${r.sta}`).join('\n');log(`Generated ${n}-leg rig.`)};
$('ofp').onclick=()=>{let ids=selected.size?[...selected]:rows.map((_,i)=>i);if(!ids.length)return log('No flights available for OFP.');ids.forEach(i=>{let r=rows[i];open(`https://dispatch.simbrief.com/options/custom?orig=${encodeURIComponent(r.dep)}&dest=${encodeURIComponent(r.arr)}&type=${encodeURIComponent(r.ac)}&reg=${encodeURIComponent(r.reg)}&fltnum=${encodeURIComponent(r.flight)}`,'_blank')});log(`Opened ${ids.length} SimBrief OFP request(s).`)};
function showAssignment(a){assignment=a;let el=$('assignment');el.classList.remove('empty');el.innerHTML=`<strong>${a.tail||'—'}</strong><div class="aircraftTitle">${a.make||'—'} ${a.model||''}</div><div class="assignmentGrid"><span>Serial</span><b>${a.serial||'—'}</b><span>Year</span><b>${a.year||'—'}</b><span>Mode-S</span><b>${(a.icao24||'—').toUpperCase()}</b><span>Operator</span><b>${a.operator||'—'}</b><span>Source</span><b>${a.source||'—'}</b></div>`}
async function api(url){let r=await fetch(url);let j=await r.json().catch(()=>({error:`HTTP ${r.status}`}));if(!r.ok)throw Error(j.error||`HTTP ${r.status}`);return j}
$('global').onclick=async()=>{try{log('Loading OpenSky live states and applying Global filters…');$('global').disabled=true;let q=new URLSearchParams({prefix:$('prefix').value,make:$('gmake').value,model:$('gmodel').value});let a=await api('/api/global/random?'+q);showAssignment(a);log(`Live assignment ${a.tail||a.icao24}: ${a.make} ${a.model}.`)}catch(e){log('Global: '+e.message);try{log('Running OpenSky connection probe…');const p=await api('/api/opensky/probe');log(`OpenSky probe: HTTP ${p.http} in ${p.elapsedMs} ms (${p.auth}).`)}catch(pe){log('OpenSky probe: '+pe.message)}}finally{$('global').disabled=false}};$('copy').onclick=()=>assignment&&navigator.clipboard.writeText(JSON.stringify(assignment,null,2));
